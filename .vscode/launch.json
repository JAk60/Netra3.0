{
    "version": "0.2.0",
    "configurations": [
    
        {
            "name": "Python Debugger: FastAPI",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "core:app",   // now just `core:app` since we cd into api/
                "--reload"
            ],
            "jinja": true,
            "cwd": "${workspaceFolder}/backend/api"
        }
    ]
}
